struct Point = {
    x: i32
    y: i32
}

struct arr[a] = {
    length: usize
    contents: ptr[a]
}

def main(args: arr[arr[i8]], something: ptr[i64]): unit = {
    val x = 10
    var y = 20
    if(x == y) {
        foo(x, y)
    }

    else {
        bar = -baz.x
    }
    while(false) {
        main()
    }
    print("Hello, world!\n")
}
