struct Point = {
    x: i32
    y: i32
}

def main(a: i32): unit = {
    val x = 10
    var y = 20
    if(x == y) {
        foo(x, y)
    }

    else {
        bar = -baz.x
    }
    while(false) {
        main()
    }
}
